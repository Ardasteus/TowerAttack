#pragma once

/**
 * Attack Types of AttackMode classes, used to simplify and differentiate between AttackMode classes without needing to cast
 */
enum AttackType
{
    AoE,
    Closest,
    Furthest
};